# Tool versions for the gtm_ops stack.
# Pinned so local devboxes and CI both use the same toolchain.

[tools]
node = "24"
# Keep in lockstep with bun-version in .github/workflows/{test,deploy-pages}.yml
bun = "1.3.14"
python = "3.12"
uv = "latest"
