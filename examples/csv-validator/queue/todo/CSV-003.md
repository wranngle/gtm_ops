---
id: CSV-003
priority: 3
labels: csv-validator,demo,bad-age
blocked_by:
---
# Validate CSV-003 (row with non-integer age)

The fixture at `examples/csv-validator/data/CSV-003.csv` has one row
whose age field is the string `thirty`. Expect the validator to exit 1
with a row-level diagnostic on stderr.
