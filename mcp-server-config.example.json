{
  "_comment": "mcp-server-config.example.json — synthetic placeholder for the gitignored mcp-server-config.json. Copy to mcp-server-config.json and fill in real settings. Gitignored because it can carry auth bindings for sibling MCP processes.",
  "server": {
    "name": "wranngle-presales-mcp",
    "version": "0.1.0",
    "transport": "stdio",
    "logLevel": "info"
  },
  "tools": {
    "presales_generate": {
      "enabled": true,
      "rateLimitPerMinute": 5
    },
    "presales_render_pdf": {
      "enabled": true,
      "rateLimitPerMinute": 10
    },
    "research_lookup": {
      "enabled": true,
      "rateLimitPerMinute": 30
    }
  },
  "auth": {
    "kind": "header",
    "headerName": "X-MCP-Auth",
    "tokenEnvVar": "MCP_SHARED_TOKEN"
  }
}
